class Player {
  constructor(id) {
    this.score = 0;
    this.id = id;
    this.turn = false;
  }
  // submitAnswer()
  // submitWager()
  // updateScore()
}



if (typeof module !== 'undefined') {
  module.exports = Player;
}