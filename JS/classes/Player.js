class Player {
  constructor(id) {
    this.score = 0;
    this.id = id;
  }
  // submitAnswer()
  // submitWager()
  // updateScore()
}



if (typeof module !== 'undefined') {
  module.exports = Player;
}